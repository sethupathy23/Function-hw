

1.Write a JavaScript function to generate an array between two integers of 1 step length.

Input :
console.log(range(3, 7));
Expected Output:
[3,4, 5, 6, 7]

var arr=[3,7];
function hoo(a,b){ // hoo is the Function Name and (a,b) is the parameter.
for (var i=0;i<arr.length;i++) //for loop for expected input and output.
if(a<b){
    return(a+1); // return keyword is the program gets stopped.we can't use return keyword outside the function.
}


}
console.log(hoo(3,7));
      
